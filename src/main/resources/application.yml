spring:
  datasource:
    url: jdbc:postgresql://<host>:5432/<db>?sslmode=require
    username: <user>
    password: <password>
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
